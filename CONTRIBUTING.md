# Contributing

Keep the deterministic core portable and independent of any coding agent. Add regression tests for security or state changes. Run `python3 -m unittest discover -s tests -v`. Treat fixtures as untrusted input and avoid network or target-project execution in tests.
