# Project Pulse

Project Pulse is an install-once Agent Skill and deterministic Python core that reports evidence-based state for any current software project. Inspect is read-only and works without initialization. Project-local JSON is optional portable state; Markdown is derived presentation.

Principles: universal over project-specific, evidence over inference, unknown over guessing, explicit writes, no target-project execution, no network or telemetry.
